# SQLite is de standaarddatabase (geen extra installatie nodig)
DATABASE_URL="file:./dev.db"

# PostgreSQL-variant (pas na de omschakeling gebruiken, zie docs/POSTGRES_MIGRATIE.md)
# DATABASE_URL="postgresql://uren_app:WACHTWOORD@localhost:5432/uren_dashboard"

# Gemini API (gebruikt voor de AI-verdeelsleutel en de AI-PV-tekst)
# Maak een sleutel via https://aistudio.google.com/app/apikey
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
